--- Buffexit.nvim's state variables
--- @class BEState
local state = {
    initialized = false, --- @type boolean
    module = "buffexit", --- @type string
    config = nil, --- @type BEConfig
}

return state
